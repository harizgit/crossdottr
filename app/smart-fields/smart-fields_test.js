'use strict';

describe('myApp.smart-fields module', function() {

  beforeEach(module('myApp.smart-fields'));

  describe('smart-fields controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var smart-fieldsCtrl = $controller('smart-fieldsCtrl');
      expect(smart-fieldsCtrl).toBeDefined();
    }));

  });
});