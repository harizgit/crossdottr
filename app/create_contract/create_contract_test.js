'use strict';

describe('myApp.create_contract module', function() {

  beforeEach(module('myApp.create_contract'));

  describe('create_contract controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var create_contractCtrl = $controller('create_contractCtrl');
      expect(create_contractCtrl).toBeDefined();
    }));

  });
});