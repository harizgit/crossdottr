'use strict';

describe('myApp.contract_list module', function() {

  beforeEach(module('myApp.contract_list'));

  describe('contract_list controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var contract_listCtrl = $controller('contract_listCtrl');
      expect(contract_listCtrl).toBeDefined();
    }));

  });
});